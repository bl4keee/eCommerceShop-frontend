import { BartekShopValidators } from './bartek-shop-validators';

describe('BartekShopValidators', () => {
  it('should create an instance', () => {
    expect(new BartekShopValidators()).toBeTruthy();
  });
});
